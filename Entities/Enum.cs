namespace Persona_work_management.Entities
{
    public enum Priority
    {
        Low,
        Medium,
        High
    }
    public enum Status
    {
        Pending,
        InProgress,
        Completed
    }
    public enum Role
    {
        User,
        Admin
    }
    public enum LabelColor
    {
        Red,
        Blue,
        Green
    }
}
